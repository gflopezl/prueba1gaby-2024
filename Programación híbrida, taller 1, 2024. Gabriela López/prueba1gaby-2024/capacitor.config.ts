import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'prueba1gaby.v2024',
  appName: 'prueba1gaby-2024',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
